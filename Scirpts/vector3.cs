using UnityEngine;
using System.Collections;

public class Vect : MonoBehaviour {

	// Use this for initialization
	void Start () {
	bool playerdievect = allint("isdead", false)
  if(playerdeadvect = true "vectdie", true)
  {
    bool vectdie = intall("isdead", dead("false",= true,"allint", isdead("true",x =(1))))
     }
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
